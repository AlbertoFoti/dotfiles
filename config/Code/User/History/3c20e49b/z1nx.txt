# Add source files
set(SOURCES
    DHS_GE.hxx
    DHS_GE.cpp
    # Add more source files as needed
)

add_subdirectory(GNC)

include_directories(GNC)
include_directories(../E2E)

# Add the library target
add_library(path_tracking_gnc ${SOURCES})

target_link_libraries(path_tracking_gnc e2e)
target_link_libraries(path_tracking_gnc navigation_module)