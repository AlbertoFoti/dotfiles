cmake_minimum_required(VERSION 3.28.0)
set(CMAKE_CXX_STANDARD 20)

file(GLOB HEADERS "*.hxx")
file(GLOB SOURCES "*.cpp")

include_directories(../MissionScenario)
include_directories(../UtilsLib)
include_directories(../Log)

# Create a library target
add_library(path_planning STATIC ${SOURCES} ${HEADERS})

target_link_libraries(path_planning mission_scenario)
target_link_libraries(path_planning utils_lib)
target_link_libraries(path_planning util_log)