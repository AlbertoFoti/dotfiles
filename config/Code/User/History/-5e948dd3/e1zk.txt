cmake_minimum_required(VERSION 3.28.0)

file(GLOB HEADERS "CoM3DOF/*.hxx")
file(GLOB SOURCES "CoM3DOF/*.cpp")

include_directories(CoM3DOF)

# Create a library target
add_library(planning_library STATIC ${SOURCES} ${HEADERS})