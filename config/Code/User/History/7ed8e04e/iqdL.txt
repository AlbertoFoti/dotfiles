# Add source files
set(SOURCES
    DHS_GE.hxx
    DHS_GE.cpp
    GNC/GE_GNC.hxx
    GNC/GE_GNC.cpp
    # Add more source files as needed
)

add_subdirectory(GNC/Guidance)

# Add the library target
add_library(path_tracking ${SOURCES})

target_link_libraries(path_tracking guidance_module)