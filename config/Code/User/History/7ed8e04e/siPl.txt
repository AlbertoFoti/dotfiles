# Add source files
set(SOURCES
    GE_GNC.hxx
    GE_GNC.cpp
    # Add more source files as needed
)

include_directories(Navigation)

# Add the library target
add_library(path_tracking ${SOURCES})

target_link_libraries(path_tracking e2e)