# Add source files
set(SOURCES
    CONFIGURATION.hxx
    # Add more source files as needed
)

include_directories(../E2E)

# Add the library target
add_library(configuration ${SOURCES})

target_link_libraries(configuration e2e)